package dev.nokee.nativeplatform.tasks;

/**
 * Mark a {@link org.gradle.process.CommandLineArgumentProvider} as task specific to avoid the provider in question from being copied to another task.
 */
public interface TaskSpecificProvider {
}
